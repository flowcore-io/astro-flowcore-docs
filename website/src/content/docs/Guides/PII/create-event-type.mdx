---
title: Create an Event Type
description: A description of how to create an event type in Flowcore with PII
sidebar:
  order: 3
---

import { Image } from "astro:assets";
import CreateEventTypeScreen1 from "../../../../assets/guides/pii/create-event-type-screen1.png";
import CreateEventTypeScreen2 from "../../../../assets/guides/pii/create-event-type-screen2.png";
import CreateEventTypeScreen3 from "../../../../assets/guides/pii/create-event-type-screen3.png";

1. In the Flowcore Console, navigate to the **Flow Type** you want to create an Event Type for.

2. Click on **Create Event Type**.
<Image src={CreateEventTypeScreen1} alt="Basic Information tab of Create Event Type dialog" />

3. Fill out the **Basic Information**:
   - **Name**: Enter the name of your event.
   - **Description**: (Optional) Provide additional context.
   - **Enable PII Masking**: Select **Yes** to enable PII.
   - **PII Key Path**: Enter the JSONPath to your PII identity field (e.g., `$.id`).

4. Switch to the **PII Configuration** tab to define your masking schema.
<Image src={CreateEventTypeScreen2} alt="PII Configuration tab for setting up masking schema" />

5. In **Schema Definition**, enter your PII masking schema in JSON.
6. Click **Generate Mock Data** to preview both your mock data and scrambled data.
<Image src={CreateEventTypeScreen3} alt="Mock Data and Scrambled Data Preview" />

7. Click **Create Event Type** to save your new event type with PII enabled.

